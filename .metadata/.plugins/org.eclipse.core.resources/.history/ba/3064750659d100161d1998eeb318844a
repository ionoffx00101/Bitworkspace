package bitjava.day01_03.Prob03;

import java.util.Arrays;

public class Student extends Person
{
	private int[] numCourses;
	private String[] courses;
	private int[] grades;

	public static void main(String[] args)
	{

	}

	public Student(String name, String address)
	{
		super(name, address);
	}

	@Override
	public String toString()
	{
		return "Student [numCourses=" + Arrays.toString(numCourses) + ", courses=" + Arrays.toString(courses) + ", grades=" + Arrays.toString(grades)
				+ "]";
	}

	public void addCourseGrade(String course, int grade)
	{

	}

	public void printGrades()
	{
		System.out.println(this); //this.toString?
		for (int i = 0; i < numCourses.length; i++)
		{
			System.out.println(courses[i] + " : " + grades[i]);

		}
		System.out.println("average : " + getAverageGrade());
	}

	public double getAverageGrade(){
		int sum=0; //
		for (int i = 0; i < numCourses.length; i++)
		{
			
		}
		return 
	}

}
